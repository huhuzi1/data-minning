Project4：决策树数据
•虹膜数据集。
问题：
•编写脚本以计算输入数据集的决策树。 
•您可以假定除最后一个属性（即类）外，所有属性都是数字属性。
 •使用基于熵的信息增益来计算每个属性的最佳分割值。 对于节点上的停止条件，如果纯度至少为95％，则停止；如果节点大小为5以下，则停止。
 •输出决策树。 请注意，在每个内部节点上，先打印决策，然后再打印“信息增益”，对于每个叶子，则打印多数标签，叶子的纯度和大小。